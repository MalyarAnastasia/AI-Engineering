model:
  type: catboost
  params:
    iterations: 400
    learning_rate: 0.1
    depth: 6
    random_seed: 42

clustering:
  n_clusters: 4
  random_state: 42

data:
  train_test_split: 0.2
  random_state: 42

api:
  host: "0.0.0.0"
  port: 8000
  reload: false
